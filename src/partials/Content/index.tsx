import Form from "../Form";
import Title from "../Title";
import * as Styled from "./styled";

const Content = () => {
  return (
    <Styled.Wrapper>
      <Form />
    </Styled.Wrapper>
  );
};

export default Content;
